{
  "name": "lobster-tank",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w packages/server\" \"npm run dev -w packages/client\"",
    "build": "npm run build -w packages/client && npm run build -w packages/server",
    "start": "node packages/server/dist/index.js",
    "typecheck": "npm run typecheck -w packages/server && npm run typecheck -w packages/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
